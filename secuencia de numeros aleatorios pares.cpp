// secuencia de numeros aleatorios pares
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<math.h>
int main(){

	int num;
	srand(time(NULL));
	int r;
	int contador =1;
	
	printf("Ingrese los valores que desee: ");
	scanf("%d", &num);
	while(contador<= num){
		
	Contador = 2;
    while ( Contador <= 10 ) {
          printf(" %d",Contador);
          Contador += 2;
		 Contador = 2;
    do {
        printf(" %d",Contador);
        Contador += 2;
    } while ( Contador <= 10 );
        printf("\n");
		
	}
	
	printf("\a");
	return 0;
}
	
